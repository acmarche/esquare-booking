<ul class="list-group">
    {% for entry in entries %}
        <li class="list-group-item">
            {{ entry.name }} {{ entry.startTime }} {{ entry.endTime }}
        </li>
    {% endfor %}
</ul>
